import fetch from 'node-fetch';

async function test() {
  // We don't have the API key available easily in node context unless we load it. Let's just create a function in douyin.ts and use the app.
}
test();
