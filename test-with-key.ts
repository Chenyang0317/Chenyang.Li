import fetch from 'node-fetch';

async function test() {
    // wait I don't know the API key. Let's read it from indexedDB or local storage? No, this runs in node. 
    // Wait, the client is storing apiKey somewhere?
}
test();
